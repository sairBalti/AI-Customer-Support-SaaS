# Core runtime — enough to start the API without business features.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic[email]>=2.9.0
pydantic-settings>=2.6.0
email-validator>=2.0.0
httpx>=0.27.0
python-multipart>=0.0.12

# Data layer
sqlalchemy[asyncio]>=2.0.36
aiomysql>=0.2.0
alembic>=1.14.0

# Security
python-jose[cryptography]>=3.3.0
passlib[argon2]>=1.7.4
argon2-cffi>=23.1.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
aiosqlite>=0.20.0
tzdata>=2024.1

# Knowledge / RAG foundation (local/dev — no paid APIs required)
pypdf>=5.0.0

# Optional infrastructure — install when implementing those adapters:
# redis>=5.0.0
# celery>=5.4.0
# chromadb>=0.5.0
