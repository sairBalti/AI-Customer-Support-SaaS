# Domain Interfaces (Ports)

Abstract repository and external-service contracts.

Infrastructure adapters implement these ports. Application services depend on the abstractions, never on concrete drivers.
