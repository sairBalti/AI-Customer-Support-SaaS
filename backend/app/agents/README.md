# AI Agents

LangGraph-based support agent pipeline:

Start → Auth → Memory → Retrieval → Prompt → LLM → Confidence → Decision → Ticket? → End

See `docs/02_Architecture.md` §9–11 and `.cursor/rules/ai-agent.mdc` / `rag.mdc`.
